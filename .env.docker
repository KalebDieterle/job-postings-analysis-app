# Copy to .env before running docker compose:
#   cp .env.docker .env
# Then fill in the real values below.

DATABASE_URL=postgresql://user:password@host/dbname
ML_SERVICE_KEY=local-dev-secret-change-me
IP_HASH_SECRET=local-dev-ip-secret
